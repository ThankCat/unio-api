# 2026-09-03 待修复问题：实施计划

## 范围

- 修正账号页近 24 小时成功率的展示口径：成功率分母只包含成功与失败，取消单独展示，在途请求单独来自 Redis 运行态。
- 为近 24 小时聚合接口补充取消请求数，避免前端从总数猜测状态。
- 将“平均延迟”改为“平均完成耗时”，明确它包含完整流式传输，并保留首字时间的独立口径。
- 说明“最近成功”同时可能由成功请求和账号检测更新，避免与 24 小时请求统计产生误解。
- 按当前生效的用量暂停阈值显示水位预警；设置读取失败时回退网关默认值。

## 本次不改

- `selected_account_id` 继续表示最终成功传输账号；取消请求的请求记录归因仍使用 `final_account_id`，不改变既有语义。
- 响应/首字超时、Fast 取消结算、TTFT 按 reasoning 分层属于已有配置或后续设计决策，不在本次口径修复中扩大范围。

## 验证

- 修改 SQL 查询后运行 `sqlc generate`。
- 对 Go 代码运行 `gofmt`，对 Admin 运行类型检查/构建与 lint（若脚本提供）。
- 运行 `git diff --check`，不连接或修改现有 PostgreSQL/Redis 数据。
